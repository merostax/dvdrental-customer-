<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    <persistence-unit name="persistent" transaction-type="JTA">

        <!-- Define your entity classes here -->
        <class>model.Address</class>
        <class>model.City</class>
        <class>model.Country</class>
        <class>model.Customer</class>
        <class>model.Payment</class>

        <properties>
            <!-- Specify the JPA provider (e.g., Hibernate) -->
            <property name="jakarta.persistence.jdbc.url" value="jdbc:postgresql://localhost:54323/customer-db"/>
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="jakarta.persistence.jdbc.user" value="postgres"/>
            <property name="jakarta.persistence.jdbc.password" value="postgres"/>

            <!-- Optional: Specify the dialect for your database -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>

            <!-- Optional: Set up JPA provider-specific properties -->
            <!-- For Hibernate -->

            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- For EclipseLink -->
            <!--<property name="eclipselink.logging.level" value="FINE"/>-->

            <!-- For OpenJPA -->
            <!--<property name="openjpa.Log" value="DefaultLevel=WARN, Runtime=INFO, Tool=INFO, SQL=TRACE"/>-->
        </properties>
    </persistence-unit>
</persistence>
